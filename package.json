{
  "name": "dsh-explore-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "Trajectory-level parallel exploration for DeepSeek Harness agents — fork N explorations, verify with execution, keep the winner.",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "license": "MIT"
}
