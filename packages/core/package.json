{
  "name": "@dsh-explore/core",
  "version": "0.1.0",
  "description": "Pure trajectory-search algorithms (best-of-N, MCTS, verdict, diff, patch) for dsh-explore",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "keywords": ["deepseek", "harness", "agent", "search", "mcts", "best-of-n"],
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
